import 'package:flutter/material.dart';

/*
 * 앱 전역으로 사용할 색상
 */

class ColorPalette {
  /// default text
  static const black = Color(0xFF282B32);

  /// white
  static const white = Color(0xFFFFFFFF);

  /// white opacity 10%
  static const blackO10 = Color(0x1A000000);

  /// red appMain
  static const appMain = Color(0xFF2971FC);

  /// disableText
  static const disableText = Color(0xFFAAB9C8);
}
