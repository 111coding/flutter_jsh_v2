import 'package:flutter/material.dart';

/*
 * 앱 전역으로 사용할 색상
 */

class ColorPalette {
  /// default text
  static const black = Color(0xFF282B32);

  static const white = Color(0xFFFFFFFF);

  static const textMain = Color.fromARGB(255, 29, 29, 66);

  static const blackO10 = Color(0x1A000000);

  static const appMain = Color(0xFF6C95FF);

  static const disableText = Color(0xFFAAB9C8);

  static const bgColor = Color(0xFFF5F6FC);
}
