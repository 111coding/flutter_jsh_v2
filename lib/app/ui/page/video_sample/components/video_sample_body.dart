import 'package:flutter/material.dart';

class VideoSampleBody extends StatelessWidget {
  const VideoSampleBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("VideoSample"),
    );
  }
}
