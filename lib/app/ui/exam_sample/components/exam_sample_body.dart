import 'package:flutter/material.dart';

class ExamSampleBody extends StatelessWidget {
  const ExamSampleBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ExamSample"),
    );
  }
}
